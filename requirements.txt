# EnterpriseIQ — Python dependencies
# Install with: pip install -r requirements.txt

# API Framework
fastapi==0.115.0
uvicorn==0.30.6

# Testing
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2        # Required by FastAPI TestClient

# Build tools (for CD artifact)
build==1.2.2
